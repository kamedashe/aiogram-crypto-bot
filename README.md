## 🚀 Запуск

### Вариант 1: Через Docker (Рекомендуется)

Самый надежный способ запуска. Бот будет работать в изолированном контейнере и автоматически перезапускаться.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/kamedashe/aiogram-crypto-bot.git
    cd aiogram-crypto-bot
    ```

2.  **Создайте файл `.env`:**
    Создайте файл с именем `.env` и добавьте туда токен вашего бота:
    ```env
    BOT_TOKEN=ВАШ_ТОКЕН_БЕЗ_ПРОБЕЛОВ
    ```

3.  **Соберите и запустите:**
    ```bash
    docker build -t crypto_bot .
    docker run -d --name my_bot --restart always --env-file .env crypto_bot
    ```

### Вариант 2: Локальный запуск (Python)

Если хотите запустить бота просто на компьютере для тестов:

1.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Настройте конфиг:**
    Создайте файл `.env` (см. пункт выше).

3.  **Запустите:**
    ```bash
    python main.py
    ```

## 📝 Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN`| Токен от @BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |

---
👨‍💻 **Developed by [Bogdan Betskov](https://freelancehunt.com/freelancer/bground.html )**
