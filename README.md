# 🤖 Aiogram Crypto & Shop Bot

Асинхронный Телеграм-бот, написанный на Python (aiogram 3.x).
Проект демонстрирует работу с базами данных (SQLite), внешними API, модульную архитектуру и контейнеризацию (Docker).

## ✨ Функционал

*   🛍 **Магазин товаров:**
    *   Вывод товаров из базы данных SQLite.
    *   Инлайн-кнопки для выбора.
    *   Динамическое редактирование сообщений (без спама).
*   📈 **Крипто-трекер:**
    *   Получение актуального курса Bitcoin через CoinGecko API.
    *   Кнопка "Обновить" для получения свежих данных в реальном времени.
*   🛡 **Безопасность:**
    *   Работа с переменными окружения (`.env`).
    *   Docker-изоляция.
*   🧠 **UX:**
    *   Обработка неизвестных команд.
    *   Понятный интерфейс.

## 🛠 Технологический стек

*   **Language:** Python 3.10+
*   **Framework:** [aiogram 3.x](https://docs.aiogram.dev/) (Asynchronous)
*   **Database:** SQLite + `aiosqlite`
*   **Network:** `aiohttp` (для запросов к API)
*   **Deploy:** Docker & Docker Compose

## 📂 Структура проекта

Проект построен на модульной архитектуре с использованием Роутеров:

```text
├── handlers/           # Обработчики сообщений (Routers)
│   ├── common.py       # Старт и обработка неизвестных команд
│   ├── crypto.py       # Логика курса валют
│   └── shop.py         # Логика магазина
├── services/           # Бизнес-логика и работа с API
│   └── crypto_api.py   # Запросы к CoinGecko
├── database.py         # Работа с SQLite (CRUD)
├── main.py             # Точка входа (Dispatcher)
├── Dockerfile          # Инструкция для сборки образа
└── requirements.txt    # Зависимости
